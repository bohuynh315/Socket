# Write a TCP/UDP application
Client sends 3 numbers a, b, c to the server. The server will calculate the solutions for the quadratic equation: ax² + bx + c = 0, then send the result back to the client.
